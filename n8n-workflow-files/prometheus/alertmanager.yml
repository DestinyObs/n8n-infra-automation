global:
  resolve_timeout: 5m

route:
  receiver: 'n8n-webhook'
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 5m

receivers:
  - name: 'n8n-webhook'
    webhook_configs:
      - url: 'http://n8n:5678/webhook/alert'
        send_resolved: true